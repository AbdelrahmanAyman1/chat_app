import 'package:flutter/material.dart';

class SplchScrren extends StatelessWidget {
  const SplchScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Chat'),
      ),
      body: const Center(
        child: Text('loading....'),
      ),
    );
  }
}
